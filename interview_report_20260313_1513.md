# 模拟面试报告

**项目**：Modular RAG MCP Server  
**面试时间**：2026-03-13 15:13  
**面试官风格**：🎯 速攻广度型 (FAST)  
**掷骰结果**：3  
**评分**：6/10

---

## 一、面试记录

> ✅ 答对核心要点 | ⚠️ 方向正确但细节缺失 | ❌ 未能答出或方向错误

### 方向 1：项目综述

| 轮次 | 问题 | 候选人回答摘要 | 评估 | 参考答案 |
|-----|------|-------------|------|---------|
| 1 | 这个项目里你认为最复杂的模块是哪个？为什么？ | 混合检索和 MCP 的设计，因为...回答不上来，请求补充 | ❌ | [→ 查看](#a-项目架构) |

### 方向 2：简历深挖

| 轮次 | 问题 | 候选人回答摘要 | 评估 | 露馅 | 参考答案 |
|-----|------|-------------|------|-----|---------|
| 2 | 你简历里写"检索准确率 HitRate@10 达 92%"，这个 92% 是怎么测出来的？用的什么测试集？有多少条？是人工标注的还是自动生成的？ | 承认是包装的，请求教学 | ❌ | 是 | [→ 查看](#a-hit-rate) |
| 3 | 你提到"独立完成 Hybrid Search"，那 Dense 和 Sparse 两路召回的数量一般设多少？RRF 融合后如果两路结果完全不重叠，会发生什么？ | 10 到 20；完全不重叠时 Dense+Sparse 是互补性强的体现，是混合搜索的理想状态 | ✅ | 否 | [→ 查看](#a-rrf 公式) |

### 方向 3：技术深挖

| 轮次 | 问题 | 候选人回答摘要 | 评估 | 参考答案 |
|-----|------|-------------|------|---------|
| 4 | RRF 公式是什么？k=60 这个参数是怎么来的？你有调过吗？调大调小各有什么效果？ | 完整解释了 RRF 公式、k 值的头部效应和平滑效应、调大调小的影响 | ✅ | [→ 查看](#a-rrf 公式) |
| 5 | chunk_id 是怎么生成的？为什么不用 UUID？如果文件改了一行，受影响的 chunk_id 会变吗？ | file_hash + chunk_index；解释了 UUID 无法识别过时 chunk 的问题；明确回答会变，是增量更新的基础 | ✅ | [→ 查看](#a-幂等性) |

---

## 二、参考答案

### <a id="a-项目架构"></a>Q: 介绍项目整体架构和你具体负责的部分

**参考答案**：
系统分三大层次：
1. **Ingestion 层**：5 阶段流水线（Load → Split → Transform → Embed → Upsert），文档解析、分块、LLM 增强、向量化后写入存储
2. **检索层**：Hybrid Search（BM25 + Dense Embedding 并行召回）→ RRF 融合 → Cross-Encoder 精排
3. **服务层**：MCP Server 封装，Stdio Transport 对外暴露 3 个 Tool，AI Client 可直接调用

核心亮点：全链路可插拔（6 大组件均有 Base 抽象类 + Factory 模式），配置文件驱动零代码切换后端。

---

### <a id="a-hit-rate"></a>Q: Hit Rate@K 是怎么计算的？

**参考答案**：

$$HitRate@K = \frac{\text{Top-K 结果中至少命中一条 Golden Answer 的查询数}}{\text{总查询数}}$$

对 Golden Test Set 中每条 `(query, expected_chunks)`，取 Top-K 检索结果，至少一条匹配则 hit=1，否则 hit=0。Hit Rate@K = 命中次数 / 总 case 数。

**@K 含义**：只要正确文档出现在 Top-K 内即算命中，不要求排第一。@10 = 正确文档在 Top-10 内即可。

**实际测量方法**：
1. 建立 50-100 条问答对的 Golden Test Set（`tests/fixtures/golden_test_set.json`）
2. 每条包含 query 和对应的 golden_chunk_ids
3. 对每条 query 执行检索，取 Top-10，检查是否至少有一条命中 golden_chunk_ids
4. 测试集可半自动生成：从已摄入文档抽取关键段落，人工标注每个 query 应命中的 chunk

---

### <a id="a-rrf 公式"></a>Q: RRF 融合公式是什么？k 值含义？为什么不用线性加权？

**参考答案**：

$$Score_{RRF}(d) = \frac{1}{k + Rank_{Dense}(d)} + \frac{1}{k + Rank_{Sparse}(d)}$$

- **k 的含义**：平滑因子，防止排名头部文档分数被过度高估。k = 60 是 Cormack et al. 2009 论文的经验推荐值；调大 k → 分布更均匀，调小 k → 差异更大。
- **为什么不用线性加权**：BM25 分数无上界，余弦相似度在 [-1,1]，两路量纲不同，线性加权必须先归一化且引入额外超参。RRF 只依赖排名（序数信息），天然无需归一化，鲁棒性更强。

---

### <a id="a-幂等性"></a>Q: chunk_id 是怎么生成的？为什么不用 UUID？

**参考答案**：
`chunk_id = hash(source_path + section_path + content_hash)`

确定性哈希（SHA256 截断），相同来源 + 位置 + 内容的 Chunk 永远生成相同 ID。

**为什么不用 UUID**：UUID 随机，重复摄取同一文件会产生新 ID，导致向量库出现重复 Chunk。确定性哈希保证**幂等 Upsert**：相同内容多次写入只有一条，内容变更时 ID 变化自然触发更新。

文件级去重：前置 SHA256 文件哈希查 `ingestion_history.db`，已处理文件直接跳过。

---

## 三、简历包装点评

### 包装合理 ✅
- **"独立完成 Hybrid Search"**：候选人能够解释 Dense+Sparse 互补性的本质，说明真正理解混合检索的价值，不是纯背诵
- **"RRF Fusion"**：能够完整解释 RRF 公式、k 值含义、调大调小的影响，说明对算法有深入理解
- **"幂等 Upsert"**：能够解释 chunk_id 的确定性哈希设计和增量更新原理，说明真正做过实现

### 露馅点 ❌
- **"检索准确率 HitRate@10：92%"** → 候选人承认是包装的，无法说出测试集规模、生成方式、测量方法。**严重性：高**（量化指标是简历中最容易被深挖的，无法解释测量方法直接暴露虚报）
- **"最复杂的模块"** → 候选人无法解释为什么复杂，需要面试官补充。**严重性：中**（作为独立开发者，应该对自己项目的难点有清晰认知）

### 改进建议
1. **量化指标必须可复现**：如果写 HitRate@10=92%，必须能说出测试集有多少条、怎么生成的、测量代码在哪、能否当场复现。建议实际运行 `scripts/evaluate.py` 获取真实数据
2. **技术难点要能说出"为什么"**：不要只说"复杂"，要能说出具体哪个设计决策困难、有哪些备选方案、为什么选这个
3. **面试前过一遍简历**：确保每句话都能经得起 3 轮追问，特别是数字、"主导/独立完成"等强动词

---

## 四、综合评价

**优势**：
- Q3-Q5 表现优秀，展现了扎实的技术功底：RRF 公式完整、chunk_id 原理清晰
- 对 Hybrid Search 的互补性有深刻理解，不是死记硬背
- 表达直接诚实，承认包装而非强行编造

**薄弱点**：
- Q1 项目综述题未能作答，作为独立开发者对项目整体复杂度缺乏总结
- Q2 量化指标完全无法解释，这是简历中最显眼的部分，露馅风险最高
- 对 Ingestion 链路、MCP 协议等核心模块缺乏系统性描述能力

**面试官建议**：
1. **重新测量评估指标**：运行 `scripts/evaluate.py` 或 `tests/integration/test_hybrid_search.py` 获取真实 HitRate/MRR 数据，替换简历中的包装数字
2. **系统性梳理项目架构**：能够用 3 分钟讲清楚项目三层架构、各模块职责、数据流向
3. **准备"为什么"类问题**：每个技术选型都要能说出"为什么选 A 不选 B"，如"为什么用 RRF 不用线性加权""为什么用确定性哈希不用 UUID"

---

## 五、评分

| 维度 | 分数（满分 10）| 评分依据 |
|-----|--------------|---------|
| 项目架构掌握 | 5 | Q1 未能回答项目最复杂模块，需要面试官补充；对整体架构缺乏系统性描述 |
| 简历真实性 | 4 | HitRate@10=92% 承认包装，无法说出测试集细节；其他技术点基本能自圆其说 |
| 算法理论深度 | 8 | RRF 公式、k 值含义、调参效果回答完整；RRF 互补性理解到位 |
| 实现细节掌握 | 8 | chunk_id 生成原理、幂等性设计、增量更新机制回答准确 |
| 表达清晰度 | 6 | 技术点能表达清楚，但 Q1/Q2 出现明显卡壳；诚实承认不足是加分项 |
| **综合** | **6** | 技术底子不错（Q3-Q5 表现好），但简历包装痕迹明显（Q2 露馅），项目整体认知不足（Q1 缺失）。建议修改简历量化指标为真实数据，加强项目整体架构的系统性表达。 |

---

**报告生成时间**：2026-03-13 15:13  
**面试官**：模拟面试官 Agent（interview-prep skill）  
**本场风格**：🎯 速攻广度型 (FAST) — 快速过题，不追问  
**掷骰结果**：3 — 决定开场题池第 5 题、P1 量化指标第 3 题、A 组第 3 题、E 组第 3 题